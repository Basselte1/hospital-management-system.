<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;
use Illuminate\Support\Facades\DB;

return new class extends Migration
{
    /**
     * Run the migrations.
     * 
     * MIGRATION 100% SÉCURISÉE
     * - Ajoute seulement une colonne (pas de modification destructive)
     * - Valeur par défaut pour les données existantes
     * - Rollback complet possible
     * - Index ajouté pour optimiser les requêtes
     */
    public function up(): void
    {
        // Vérifier d'abord si la colonne n'existe pas déjà
        if (!Schema::hasColumn('consultations', 'is_initial')) {
            Schema::table('consultations', function (Blueprint $table) {
                // Ajout de la colonne avec valeur par défaut
                // Les consultations existantes seront automatiquement à false
                $table->boolean('is_initial')
                    ->default(false)
                    ->after('type_intervention')
                    ->comment('Indique si c\'est une consultation initiale d\'enregistrement');
                
                // Index pour optimiser les requêtes de filtrage
                $table->index('is_initial', 'idx_consultations_is_initial');
            });

            // Log du succès
            DB::statement("
                INSERT INTO information_schema.PROCESSLIST 
                SELECT 'Migration is_initial: SUCCESS' 
                WHERE FALSE
            ");
        }
    }

    /**
     * Reverse the migrations.
     * 
     * Rollback complet et sécurisé
     */
    public function down(): void
    {
        if (Schema::hasColumn('consultations', 'is_initial')) {
            Schema::table('consultations', function (Blueprint $table) {
                // Supprimer d'abord l'index
                $table->dropIndex('idx_consultations_is_initial');
                
                // Puis supprimer la colonne
                $table->dropColumn('is_initial');
            });
        }
    }
};